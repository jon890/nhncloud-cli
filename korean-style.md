# korean-style — nhncloud-cli

전역 `~/.claude/CLAUDE.md` 한국어 표현 정책의 프로젝트별 구체화.
외래어 매핑 표·문장 종결 규칙·자가 점검 항목을 이 파일에 단일 소스로 둔다.
docs·응답·skill 작성 모두에 적용한다. 기술 식별자(폴더명·슬래시 커맨드·코드 심볼·경로)는 그대로 둔다.

## 외래어 매핑 표

한국 사용자가 한 번에 의미를 파악하기 어려운 음차 외래어는 우리말로 바꾼다.
맥락에 따라 대체어가 갈리면 표의 우선 대체어를 기본으로 한다.

| 금지 외래어 | 대체어 | 비고 |
|------|------|------|
| 매트릭스 | 표 | matrix |
| 게이트 | 점검 | 맥락 따라 "승인 단계" / "통과 조건" |
| 트리아지 | 분류 | "우선순위 선별" |
| 베이스라인 | 기준선 | 비교 기준값 |
| 스파이크 | 급증 | 그래프 맥락 / 개발 맥락은 "사전 기술 검증" |

새 음차 외래어를 발견하면 이 표에 행을 추가한다.

## 문장 종결 규칙

- 본문 평문 문장은 동사로 끝맺어 의미를 완결한다(명사형 종결 회피).
  - 금지: "측정 필요.", "동작 불변."
  - 권장: "측정한다.", "동작이 변하지 않는다."
- 예외 — bullet list / 표 셀 / 헤더 / 부가 설명 안의 항목은 명사구 종결을 허용한다.

## 자가 점검

작성 직후 다음을 grep 으로 확인한다.

```bash
# 금지 외래어 (기술 식별자·이 파일 자신 제외)
grep -rnE "매트릭스|게이트|트리아지|베이스라인|스파이크" docs/ README.md skills/ CLAUDE.md \
  | grep -v "korean-style.md"   # 기대: 0건
```

- 한 paragraph 에 콤마 3+ 로 항목을 나열한 곳이 있는가 → 리스트로 변환.
- 본문에 `A + B` / `A · B` / `A & B` 인라인 연결이 있는가 → bullet list 로 변환.
- 평문 문장이 명사형("필요.", "불변.")으로 끝나는가 → 동사로 풀이.
- 표 셀에 정보 4+ 가 압축돼 있는가 → `<br>` 로 분리.
